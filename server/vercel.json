{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "dist/api/index.js" }
  ],
  "crons": [
    { "path": "/api/backup/cron", "schedule": "0 2 * * *" }
  ]
}
